*.db
*.db-wal
*.db-shm
.DS_Store
claude_desktop_config.json
whatsapp.log

# Binario compilado del bridge (artefacto de build). Cubre el nombre explícito
# (-o whatsapp-bridge) y el del módulo (whatsapp-client) que produce `go build`.
whatsapp-bridge/whatsapp-bridge
whatsapp-bridge/whatsapp-client

# Guía de trabajo INTERNA (roadmap/log de desarrollo). No es documentación pública
# ni va al repo de publicación; se mantiene solo en local.
AUDIT.md

# Datos de runtime del bridge: DBs (mensajes/sesión), token, y MEDIA descargada
# (fotos/audios/videos/documentos/stickers privados). NADA de store/ debe versionarse.
whatsapp-bridge/store/

# Artefactos de herramientas locales (no versionar)
.claude/
.atl/
.ruff_cache/

# Artefactos de GoReleaser (builds locales de snapshot)
dist/

# Codex CLI (herramienta personal, no del proyecto)
.codex/
AGENTS.md
