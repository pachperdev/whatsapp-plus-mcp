*.db
claude_desktop_config.json
whatsapp.log

# Binario compilado del bridge (artefacto de build)
whatsapp-bridge/whatsapp-bridge

# Config MCP local (rutas absolutas de la máquina, no portable)
.mcp.json
